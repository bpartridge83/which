ironWorker = require('./lib/client');

client = new ironWorker.Client()

client.tasksCreate('testss', {}, {}, (error, body) ->
  console.log(body)

  client.tasksWaitFor(body.id, {}, (error, body) ->
    console.log(body))
)
